{
  "name": "dns_cache",
  "version": "1.0.0",
  "description": "Microservice caching DNS querries. Child and part of Secure DNS Cloud Service microservice.",
  "main": "app.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "tap test/**/*.test.js",
    "start": "fastify start -l info app.js",
    "dev": "fastify start -w -l info -P app.js"
  },
  "keywords": [],
  "author": "Buster Wärn, Tony Berglund",
  "license": "ISC",
  "dependencies": {
    "boom": "^7.3.0",
    "fastify": "^2.0.0",
    "fastify-autoload": "^1.0.0",
    "fastify-cli": "^1.5.0",
    "fastify-plugin": "^1.5.0"
  },
  "devDependencies": {
    "tap": "^12.5.3"
  }
}
